;;; init-defaults.el --- Startup defaults -*- lexical-binding: t; -*-

;;; Commentary:
;; Owns package-independent defaults as they are migrated from legacy files.

;;; Code:

(provide 'init-defaults)
;;; init-defaults.el ends here
